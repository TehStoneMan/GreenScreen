package io.github.tehstoneman.greenscreen.client.model;

public class BrightModelLoader// implements IModelLoader< BrightModel >
{
	/*
	 * public static final BrightModelLoader INSTANCE = new BrightModelLoader();
	 *
	 * private final Map< BrightModel.ModelSettings, BrightModel > modelCache = Maps.newHashMap();
	 * private final Map< ResourceLocation, MaterialLibrary > materialCache = Maps.newHashMap();
	 *
	 * private ResourceManager manager = Minecraft.getInstance().getResourceManager();
	 *
	 * @Override
	 * public void onResourceManagerReload( ResourceManager resourceManager )
	 * {
	 * modelCache.clear();
	 * materialCache.clear();
	 * manager = resourceManager;
	 * }
	 *
	 * @Override
	 * public BrightModel read( JsonDeserializationContext deserializationContext, JsonObject modelContents )
	 * {
	 * GreenScreen.LOGGER.info( "==== Read Model {} {} ====", deserializationContext, modelContents );
	 * // TODO Auto-generated method stub
	 * return null;
	 * }
	 */
}
